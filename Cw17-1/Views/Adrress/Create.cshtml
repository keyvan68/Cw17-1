@model Cw17_1.Models.ViewModels.AddressViewModel

@{
    ViewData["Title"] = "اضافه کردن ادرس";
}

<h3>اضافه کردن ادرس</h3>


<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-controller="Adrress" asp-action="Create" method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>

            <div class="form-group">
                <label asp-for="Name" class="control-label"></label>
                <input asp-for="Name" class="form-control" />
                <span asp-validation-for="Name" class="text-danger"></span>
                
            </div>
            <input asp-for=userId value="@ViewBag.UserId" type="hidden" />
            <div class="form-group">
                <input type="submit" value="ثبت" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-controller="User" asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}

